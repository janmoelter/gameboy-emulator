
#include <iostream>
#include <iomanip>
#include <fstream>

#define hex(n) std::right << std::setw(n) << std::setfill('0') << std::hex << std::uppercase
#define dec(n) std::right << std::setw(n) << std::setfill(' ') << std::dec << std::uppercase

#define STD_COUT__LINE std::cout << __LINE__ << std::endl;

#include <cstdint>

#include <getopt.h>

#include <iterator>

#include <array>
#include <vector>
#include <span>
#include <map>
#include <set>

#include <algorithm>

#include <bit>

#include <chrono>

#include <thread>
#include <future>

#include <variant>

#include <functional>

#include <exception>

#include <filesystem>
